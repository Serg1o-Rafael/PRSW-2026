//Declaração de variáveis

let valor, modulo

valor = 10
modulo = valor % 2 // modulo é o resto da divisão 
if(modulo == 0) {
    console.log( valor +" é par!")
}
else{
    console.log(valor + " é impar!" )
}